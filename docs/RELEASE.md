# LAAS CMS — История релизов
## v0.1 → v2.3.10 (для людей, а не для роботов)

Этот документ — **история взросления LAAS CMS**:
от раннего прототипа до зрелой, production-ready платформы с полноценной демонстрацией возможностей.

Без маркетинга. Без воды. Про то, **что реально появлялось и зачем**.

---

## 🌱 v0.x — Рождение идеи

### v0.1 – v0.9 (предыстория)
**Статус:** прототип / исследование

- Kernel → Router → Controller → View
- Отказ от фреймворков (осознанно)
- Модульная структура
- HTML-first подход
- Bootstrap как UI-база
- Первые admin-экраны

📌 *Цель:* доказать, что простая CMS без фреймворков жизнеспособна.

---

## 🚧 v1.0 – v1.6 — Основа системы

### Основные вехи
- RBAC (роли и права доступа)
- Audit Log (журнал аудита)
- i18n (интернационализация)
- Разделение Admin / Frontend
- DevTools (только для разработки)
- Validation layer (слой валидации)
- CLI и миграции

📌 *Система стала использоваться как CMS, а не демо.*

---

## 🖼 v1.7 – v1.9 — Media как полноценная функция

### v1.7.x
- DevTools полировка
- Улучшения UX админки

### v1.8.x — Загрузка медиафайлов
- Защищённые загрузки:
  - MIME sniffing (определение типа)
  - Quarantine flow (карантин)
  - Ограничения по размеру
  - Интеграция ClamAV (антивирус)
- SHA-256 хеш + дедупликация
- RBAC + Audit для медиа
- HTMX-интерфейс

### v1.9.x — Обработка медиа
- Предгенерация миниатюр (sm/md/lg)
- Детерминированный вывод
- Защита изображений (удаление метаданных, лимит пикселей)
- Media Picker (переиспользуемый HTMX-компонент)

📌 *Media перестала быть "прикрученной фичей" — стала сильной стороной CMS.*

---

## ⚙️ v1.10 – v1.11 — Готовность к продакшену

### v1.10.x — Облачное хранилище
- Режим публичных медиа
- Подписанные URL (временный доступ к приватным файлам)
- Драйвер S3 / MinIO
- SigV4 аутентификация

### v1.11.0 — Операционная стабильность
- Эндпоинт `/health`
- Режим read-only (для обслуживания)
- Backup / Restore через CLI
- Проверка конфигурации
- Защита от параллельного восстановления

### v1.11.1 – v1.11.3 — Укрепление продакшена
- Полировка безопасности операций (dry-run, подтверждения)
- Укрепление backup/restore (контрольные суммы, откат)
- Документация для продакшена (чеклист развёртывания)
- Путь обновления (версионирование, миграции)
- Известные ограничения (честно про границы возможностей)

📌 *Система стала безопасной для реального production.*

---

## 🤖 v1.12 — CI / QA / Автоматизация релизов

**Автоматизация и контроль качества кода.**

- GitHub Actions CI
  - PHPUnit тесты
  - PHP-CS-Fixer линтер
  - SQLite smoke-тесты
- CLI-команда `ops:check`
- Автоматизация релизов из git-тегов
- Автогенерация release notes из VERSIONS.md

📌 *Ручные релизы и "забыли прогнать тесты" ушли в прошлое.*

---

## 🚀 v1.13 – v1.15 — Зрелость платформы

### v1.13 — Производительность и кэширование
- Файловый кэш для настроек и меню
- Хуки инвалидации при изменениях
- Кэширование i18n на запрос
- CLI для прогрева шаблонов (`templates:warmup`)
- Производительность стала предсказуемой

### v1.14 — Поиск (админка + фронтенд)
- Поиск страниц (фронтенд + админка)
- Поиск медиа (админка)
- Поиск пользователей (админка)
- HTMX live search с задержкой
- Безопасная подсветка (защита от XSS)
- Индексы для производительности поиска

### v1.15 — Зрелость прав и аудита
- Группы прав (организация по модулям)
- Клонирование ролей (быстрое создание)
- Аудит изменений RBAC
- Фильтры в интерфейсе аудита (пользователь/действие/даты)

📌 *CMS стала удобной не только для пользователей, но и для администраторов.*

---

## 🏁 v2.0.0 — Стабильный релиз CMS

**Ключевой момент проекта.**

### Что означает v2.0
- Архитектура **зафиксирована**
- Контракты стабильны
- Гарантируется обратная совместимость
- Debug-функции исключены из прода (`DevTools` только при `APP_DEBUG`)
- Проверки релиза (CLI-команда `release:check`)

### Критерии готовности
- Готовые к продакшену операции
- Backup/restore протестированы
- CI зелёный
- Документация полная
- Технического долга нет

📌 *С этого момента LAAS — платформа, а не эксперимент.*

---

## 🔧 v2.1.x — UX и операционная прозрачность

### v2.1.0 — Снимок конфигурации
- CLI-команда `config:export`
- Безопасный JSON-снимок рабочей конфигурации
- Затирание секретов (токены, пароли)
- Полезно для поддержки и сравнения окружений
- Включены флаги storage/media/security

### v2.1.1 — Глобальный поиск в админке
- Единый поиск в админке (`/admin/search`):
  - Страницы (заголовок/содержимое)
  - Медиа (имя файла/MIME)
  - Пользователи (логин/email)
- HTMX UX с живыми результатами
- Учитывает RBAC-права
- Скрывает недоступные разделы

📌 *Админам стало быстрее и спокойнее работать.*

---

## 🛡 v2.2.x — Контроль, гарантии и стабильность

### v2.2.0 — Диагностика RBAC
- Интроспекция прав (`/admin/diagnostics`):
  - Кто имеет права
  - Почему (через какие роли)
  - Эффективные права с объяснениями
- Диагностика без догадок
- Аудит просмотра диагностики

### v2.2.1 — Контрактные тесты
- Контрактные тесты для:
  - Обнаружения модулей (JSON-схема)
  - Контракта хранилища (put/get/delete)
  - Контракта медиа (раздача, подписанные URL)
- Защита архитектуры v2.0 от регрессии
- Основа для сторонних модулей
- Защита архитектурных инвариантов

### v2.2.2 — Критичная производительность
- Индексы производительности:
  - Pages (slug, published_at)
  - Media (sha256, created_at)
  - Audit (user_id, created_at)
- Исправление N+1 запросов (батчинг ролей в списке пользователей)
- Базовый кэш запросов для:
  - Настроек (TTL + инвалидация)
  - Прав (кэш RBAC)
  - Меню (с учётом локали)
- Контроль количества запросов в тестах

### v2.2.3 — Документация OPcache + процесс развёртывания
- Рекомендации OPcache для продакшена (`docs/OPCACHE.md`)
- Безопасный процесс развёртывания PHP-FPM (`docs/DEPLOY.md`)
- Руководство по перезагрузке (graceful vs hard restart)
- Заметки о безопасности opcache_reset()
- Лучшие практики продакшен-развёртывания

### v2.2.4 — Отчёты покрытия + CI-порог
- Отчёты покрытия PHPUnit:
  - Clover XML (машиночитаемый)
  - HTML-отчёт (для людей)
- CI-порог покрытия (минимум 17% покрытия строк)
- Артефакты покрытия в GitHub Actions
- Расширенные тесты для ядра и критичных путей
- Визуализация покрытия для команды

### v2.2.5 — Набор тестов регрессии безопасности
- Выделенная PHPUnit-группа безопасности (`@group security`)
- Тесты регрессии безопасности:
  - Безопасность auth-сессий (ротация, rate limit)
  - Защита CSRF (валидация токенов)
  - Path traversal (медиа, миниатюры)
  - SQL-инъекции (экранирование поиска)
  - XSS (контент, подсветка)
  - Безопасность загрузок (подмена MIME, лимиты размера)
  - SSRF (валидация S3-эндпоинтов)
  - Open redirect (login/logout)
  - Rate limiting (логин, API)
  - Соблюдение RBAC
  - Подписанные URL (подпись, срок действия, область)
  - Безопасность операций (read-only, валидация резервных копий)
- CI-задача для security-тестов
- Документация по тестированию безопасности (`docs/TESTING.md`)

### v2.2.6 — Абстракция сессий
- `SessionInterface` — контракт для сессий
- `PhpSession` — продакшен-реализация
- `InMemorySession` — тестовая реализация
- `Request::session()` — внедрение зависимости
- Удалено прямое использование `$_SESSION`
- Улучшена тестируемость (изолированные unit-тесты)
- Готовность к будущим бэкендам (Redis/Memcached)
- Миграция: 46 файлов, +676/-393 строки

📌 *Система защищена от "случайной деградации" и готова к росту.*

---

## 🎨 v2.3.x — Showcase и демонстрация возможностей

### v2.3.0 — Витрина на главной (интеграционное демо)

**Главная страница как полноценная демонстрация системы.**

#### Что изменилось
Домашняя страница (`/`) превратилась в **read-only витрину** всех возможностей системы:

**Блоки демонстрации:**
1. **Статус системы/операций**
   - Версия, APP_ENV, режим read-only
   - Драйвер хранилища (Local/S3)
   - Статус кэша
   - Режим медиа (приватный/публичный/подписанный)

2. **Модуль страниц**
   - Последние страницы (заголовок, slug, отрывок)
   - HTMX live search
   - Реальные данные из PagesRepository

3. **Библиотека медиа**
   - Последние 6 медиафайлов
   - Миниатюры изображений
   - Бейджи для не-изображений
   - Индикаторы публичный/приватный

4. **Меню**
   - Реальная отрисовка меню (активное состояние)
   - Учёт локали (интеграция i18n)
   - Интеграция MenuService

5. **Поиск**
   - Единый поиск на фронтенде
   - Результаты по страницам + медиа
   - HTMX live search
   - Безопасная подсветка

6. **Снимок Auth/RBAC**
   - Состояние: гость/авторизован
   - Список ролей
   - Количество прав
   - Кнопки действий (с учётом RBAC)

7. **Журнал аудита**
   - Последние 5 событий аудита
   - Действие + Актор + Время
   - Доступ по праву (`audit.view`)

8. **Панель производительности** (ТОЛЬКО ДЛЯ РАЗРАБОТКИ)
   - Количество запросов
   - Попадания/промахи кэша
   - Время отрисовки
   - Видна только при `APP_DEBUG=true`

#### Технические детали
- **HomeController** — агрегация данных через сервисы
- **Партиалы шаблонов** — `home/_system.html`, `home/_pages.html`, и т.д.
- **Bootstrap 5 + HTMX** — адаптивные карточки, живые обновления
- **Соблюдение RBAC** — скрытие блоков без прав (не disabled)
- **Переключатели конфигурации** — блоки можно отключать
- **Без операций записи** — 100% read-only
- **Без фейковых данных** — только реальные модули и данные

#### Зачем это нужно
- **Демонстрация:** новые пользователи сразу видят возможности
- **Интеграционный тест:** главная как UX-тест всех модулей
- **Онбординг:** быстрое понимание структуры CMS
- **Витрина для продакшена:** можно показывать клиентам "что внутри"

📌 *LAAS CMS теперь демонстрирует себя сама, без внешних демо-страниц.*

---

### v2.3.1 — Полировка UX главной страницы

**Улучшения визуальной иерархии:**
- Улучшенная раскладка первого экрана
- Панель статуса (health, кэш, режим медиа)
- Блоки страниц/медиа на основе list-group с live search
- Единый глобальный поиск (страницы + медиа)
- Интерактивная витрина медиа с hover-эффектами
- Журнал аудита с цветными бейджами действий
- Панель производительности (только для debug)
- Empty states для всех блоков контента
- Матрица зрелости функций

📌 *Главная страница стала полноценной визиткой системы.*

---

### v2.3.2 — Модуль Changelog

**Git-интеграция для журнала изменений:**
- Фронтенд-лента изменений с пагинацией (`/changelog`)
- Админ-настройки, тест источника, превью, очистка кэша
- GitHub API и локальный git-провайдер с кэшем
- Настраиваемый путь к git-бинарнику
- Улучшенное логирование ошибок git

**Зачем:**
- Прозрачность для пользователей (видят историю обновлений)
- Автоматизация документирования изменений
- Выбор источника (GitHub API или локальный git)

📌 *Теперь история разработки доступна прямо из интерфейса.*

---

### v2.3.3-v2.3.10 — API v1 и безопасность

**Headless CMS и API-first подход.**

#### v2.3.3 — REST API v1 (базовая версия)
- REST API v1 с единым форматом ответов
- Bearer-токены + RBAC + аудит
- API rate limit bucket и CORS allowlist
- Админ-UI для управления API-токенами
- Исправление race condition в кэше настроек

#### v2.3.4 — Отзыв и истечение токенов
- Колонка `revoked_at`
- Проверка истечения срока
- Эндпоинт `/api/v1/auth/revoke`
- Статус-бейджи в админке

#### v2.3.5 — Аудит аутентификации с защитой от спама
- События аудита для создания/отзыва токенов
- Аудит неудачных попыток входа (с защитой от флуда)
- Rate-limiting по IP/префиксу токена

#### v2.3.6 — Выделенная политика rate limit для API
- Бакеты по токену/IP
- Конфигурация per-minute + burst
- Заголовок `Retry-After` при 429
- Заголовки: `X-RateLimit-Limit/Remaining/Reset`

#### v2.3.7 — Строгий CORS allowlist
- Deny по умолчанию
- Разрешённые origins/методы/заголовки, max-age
- Валидация preflight (Origin + метод + заголовки)
- Запрет wildcards с Authorization

#### v2.3.8 — Гигиена секретов
- Запрет логирования Authorization-заголовков
- Маскирование токенов в DevTools
- Админ-список токенов скрывает хеши/plaintext (показ один раз при создании/ротации)

#### v2.3.9 — Ротация токенов
- Админ-действие для ротации токена (copy-once, опциональный отзыв старого)
- Руководство по ротации/отзыву в API/PRODUCTION docs

#### v2.3.10 — Тестовые наборы API/безопасности + CI
- PHPUnit группа `@group api` (токены, auth-ошибки, rate limits, CORS)
- GitHub Actions задача `api-tests` (артефакт junit-api)
- Обновлённая документация по тестированию API/security групп

**Технические детали:**
- **Аутентификация:** SHA-256 хеширование токенов
- **RBAC:** Права `api.tokens.view`, `api.tokens.manage`, `api.tokens.revoke`
- **Rate Limiting:** 60 req/min (настраивается), burst 10
- **CORS:** Строгий allowlist, поддержка preflight
- **Аудит:** Все операции с токенами + неудачные попытки входа
- **Безопасность:** Маскирование, отзыв, истечение, ротация

**Зачем это нужно:**
- **Headless CMS:** Разделение фронтенда и бэкенда
- **Интеграции:** API для мобильных приложений, SPA, внешних сервисов
- **Безопасность:** Контроль доступа, аудит, защита от злоупотреблений
- **Прозрачность:** История изменений через Changelog
- **DevOps:** Мониторинг, логирование, health checks через API

📌 *LAAS CMS теперь полноценный headless CMS с производственно-готовым API.*

---

## 🧭 Итог: v0.1 → v2.3.10

LAAS CMS прошла путь от идеи до зрелой платформы:
- **63+ релиза** от прототипа к продакшену
- **v0.x:** исследование и proof-of-concept
- **v1.x:** укрепление продакшена и зрелость функций
- **v2.0:** стабильная платформа с гарантиями
- **v2.1-2.2:** UX, производительность, безопасность, качество
- **v2.3:** витрина возможностей + headless API + интеграции

### Что отличает LAAS CMS

**Философия:**
- Минимум магии, максимум предсказуемости
- Честные ограничения (документированы в `docs/LIMITATIONS.md`)
- Уважение к админам и DevOps
- Контроль вместо хаоса

**Технические принципы:**
- Без фреймворков (PHP 8.4+, PDO, никаких ORM)
- HTML-first шаблоны (компилируются в кэш)
- Безопасность на первом месте (CSRF, rate limit, RBAC, аудит)
- Контрактная архитектура (защищена тестами)
- Read-only операции где возможно
- Плавная деградация (фоллбэки для БД/кэша)

**Операционная зрелость:**
- CI/CD (GitHub Actions)
- Проверки здоровья (`/health`)
- Backup/Restore (протестировано и документировано)
- Режим read-only (окна обслуживания)
- Мониторинг производительности (DevTools)
- Тесты регрессии безопасности
- Контрактные тесты (архитектурные гарантии)

**Опыт разработчика:**
- Без сборки (Bootstrap 5 + HTMX)
- Прогрессивное улучшение HTMX
- i18n (15+ языков)
- CLI-инструменты для всех операций
- Всеобъемлющая документация
- Чёткие пути обновления

### Для кого LAAS CMS

✅ **Подходит:**
- Небольшие и средние проекты
- Команды, ценящие простоту и контроль
- Проекты с долгой жизнью (годами)
- Администраторы без знания PHP
- DevOps, которые хотят спокойствия

❌ **Не подходит:**
- Enterprise с legacy-требованиями
- Проекты, требующие горизонтального масштабирования на десятки серверов (см. `docs/LIMITATIONS.md`)
- Команды, зависимые от конкретного фреймворка

### Что дальше

LAAS CMS достигла зрелости. Дальнейшее развитие:
- Улучшение UX существующих функций
- Расширение тестирования безопасности
- Оптимизация производительности
- Модули сообщества (на основе контрактных тестов)

**Это CMS, которую не страшно поддерживать годами.**

---

- **Последнее обновление:** январь 2026
- **Текущая версия:** v2.3.10
- **Лицензия:** MIT
- **Автор:** Eduard Laas
- **E-Mail:** info@laas-cms.org
- **Сайт:** https://laas-cms.org
