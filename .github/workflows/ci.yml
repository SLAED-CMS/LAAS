name: CI

on:
  push:
    branches:
      - main
      - 'release/*'
  pull_request:
    branches:
      - main
      - 'release/*'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
      - run: composer install --no-interaction --prefer-dist
      - run: composer validate --no-check-publish
      - name: PHP lint
        run: |
          find src modules tools database public -name '*.php' -print0 | xargs -0 -n1 php -l
      - name: Debt sweep
        run: |
          if rg -n "TODO|FIXME" src modules tools; then
            echo "TODO/FIXME found"
            exit 1
          fi

  test:
    runs-on: ubuntu-latest
    env:
      APP_ENV: test
      DB_DRIVER: sqlite
      DB_CONNECTION: sqlite
      DB_DATABASE: ':memory:'
      DB_NAME: ':memory:'
      CI_TEMPLATES_WARMUP: ${{ vars.CI_TEMPLATES_WARMUP }}
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
      - run: composer install --no-interaction --prefer-dist
      - run: vendor/bin/phpunit --log-junit junit.xml
      - name: Templates warmup
        if: env.CI_TEMPLATES_WARMUP == 'true'
        run: php tools/cli.php templates:warmup
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: junit
          path: junit.xml

  coverage:
    runs-on: ubuntu-latest
    env:
      APP_ENV: test
      DB_DRIVER: sqlite
      DB_CONNECTION: sqlite
      DB_DATABASE: ':memory:'
      DB_NAME: ':memory:'
      COVERAGE_MIN_LINES: 35
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          coverage: pcov
      - run: composer install --no-interaction --prefer-dist
      - run: vendor/bin/phpunit --coverage-clover coverage/clover.xml --coverage-html coverage/html
      - name: Coverage threshold
        run: |
          php - <<'PHP'
          <?php
          $file = 'coverage/clover.xml';
          if (!file_exists($file)) {
              fwrite(STDERR, "Missing clover.xml\n");
              exit(1);
          }
          $xml = simplexml_load_file($file);
          $rate = 0.0;
          if ($xml !== false) {
              if (isset($xml->project['line-rate'])) {
                  $rate = (float) $xml->project['line-rate'];
              } elseif (isset($xml['line-rate'])) {
                  $rate = (float) $xml['line-rate'];
              } else {
                  $node = $xml->xpath('//project');
                  if ($node && isset($node[0]['line-rate'])) {
                      $rate = (float) $node[0]['line-rate'];
                  }
              }
          }
          $percent = $rate * 100;
          $threshold = (float) getenv('COVERAGE_MIN_LINES');
          echo 'Line coverage: ' . number_format($percent, 2) . "% (threshold {$threshold}%)\n";
          if ($percent < $threshold) {
              exit(1);
          }
          PHP
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-clover
          path: coverage/clover.xml
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-html
          path: coverage/html

  smoke-sqlite:
    runs-on: ubuntu-latest
    env:
      APP_ENV: test
      DB_DRIVER: sqlite
      DB_CONNECTION: sqlite
      DB_DATABASE: ${{ github.workspace }}/storage/tmp/ci.sqlite
      DB_NAME: ${{ github.workspace }}/storage/tmp/ci.sqlite
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
      - run: composer install --no-interaction --prefer-dist
      - run: |
          mkdir -p storage/logs storage/sessions storage/cache storage/backups storage/tmp storage/uploads
      - run: php tools/cli.php ops:check
      - run: php tools/cli.php migrate:up
      - run: php tools/cli.php migrate:status

  release-check:
    runs-on: ubuntu-latest
    env:
      APP_ENV: test
      DB_DRIVER: sqlite
      DB_CONNECTION: sqlite
      DB_DATABASE: ${{ github.workspace }}/storage/tmp/release.sqlite
      DB_NAME: ${{ github.workspace }}/storage/tmp/release.sqlite
      RELEASE_CHECK_DB_DRIVER: pdo
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
      - run: composer install --no-interaction --prefer-dist
      - run: |
          mkdir -p storage/logs storage/sessions storage/cache storage/backups storage/tmp storage/uploads
      - run: php tools/cli.php migrate:up
      - run: php tools/cli.php release:check
